Instructions for the moment:
ssh-keygen -t ed25519 -f .ssh/terraform-lab -N ""

To create the infra:
docker compose run --rm terraform -c "terraform init"
docker compose run --rm terraform -c "terraform plan"
docker compose run --rm terraform -c "terraform apply"
docker compose run --rm terraform -c "TF_VAR_k8s_enabled=true terraform apply"

To destroy the infra:
docker compose run --rm terraform -c "TF_VAR_k8s_enabled=true terraform destroy"
docker compose run --rm terraform -c "terraform destroy"
docker compose run --rm terraform -c "terraform destroy -refresh=false"
